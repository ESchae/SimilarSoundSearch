TEST_CMD = python -m doctest
CHECKSTYLE_CMD = flake8

all: compile test checkstyle clean

compile:
	@echo "Nothing to compile for Python"

test:
	$(TEST_CMD) *.py

checkstyle:
	$(CHECKSTYLE_CMD) *.py

clean:
	rm -f *.pyc
	rm -rf __pycache__
	rm testfiles/empty.db
	rm *.log
